#include "constant.h"

#include "state.nxc"
#include "movement.nxc"
#include "arm.nxc"
#include "calibration.nxc"

//#define CALIB

task main() {
    SetSensorTouch(IN_1);
    SetSensorLowspeed(IN_2);
    SetSensorUltrasonic(IN_3);
    SetSensorLight(IN_4);
#ifdef CALIB
    calibrate_arm();
    horiz();
#else
    init_pos();
    init_arm();
    init_calibrate();
    mainLoop();
#endif
}
