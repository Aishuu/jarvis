#include "constant.h"

#include "movement.nxc"
#include "detect_ball.nxc"

task main() {
    SetSensorLowspeed(IN_4);
    SetSensorUltrasonic(IN_3);
    init_pos();
    int xmax = 40;
    int ymax = 50;
    int ymin = -18;
    //find_ball();
    //rotate(80);
    //grab_and_send_ball(-28, 25);
    start task_hold;
    moveTo(0, ymin);
    moveTo(-xmax, ymin);
    moveTo(-xmax, ymax);
    moveTo(xmax, ymax);
    moveTo(xmax, ymin);
    moveTo(0, ymin);
    moveTo(0, 0);
}
