#include "constant.h"

#include "movement.nxc"
#include "detect_ball.nxc"
#include "arm.nxc"

task main() {
    SetSensorTouch(IN_1);
    SetSensorLowspeed(IN_2);
    SetSensorUltrasonic(IN_3);
    SetSensorLight(IN_4);
    init_pos();
}
